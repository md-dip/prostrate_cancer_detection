# Core
flask==3.0.3
gunicorn==22.0.0
pillow==10.4.0
numpy==1.26.4

# Vision / preprocessing
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
scikit-learn==1.5.1

# Torch — CPU build for Railway. For local GPU use the CUDA wheel instead.
torch==2.3.1
torchvision==0.18.1
timm==1.0.9

# Geometric (must match torch 2.3.1)
torch-geometric==2.5.3
